<%- include header %>
<% posts.forEach(function (post, index) { %>
  <p><h2><a href="u/<%= post.time.minute %>/<%= post.title %>"><%= post.title %></a></h2></p>
  <p class="info">
    日期：<%= post.time.minute %>
  </p>
  <!-- <p><%- post.post %></p> -->
<% }) %>
<%- include paging %>
<%- include footer %>